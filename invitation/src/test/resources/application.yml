spring:
  jpa:
    show-sql: true
  cloud:
    aws:
      credentials:
        profile:
          name: ${TEST_AWS_PROFILE}

homeparty:
  datasource:
    main:
      driver-class-name: com.mysql.cj.jdbc.Driver
      jdbc-url: jdbc:mysql://127.0.0.1:38291/homeparty_main_test
      username: root
      password: mysql
      maximum-pool-size: 3


