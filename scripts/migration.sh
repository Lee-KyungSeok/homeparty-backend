#!/bin/bash

# 만들어야 할 스크립트
# 1. create migration file  (flyway format, seed 및 migration 파일, domain 지정)
# 2. start migration with env (flyway)
# 3. clean migration with env  (flyway)
# 4. add seed with env  (flyway)
# 5. reset (clean + start + seed)  (flyway)